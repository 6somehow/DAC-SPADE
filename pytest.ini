[pytest]
testpaths = tests
pythonpath = python
markers =
    cuda: requires CUDA runtime
    sm90a: requires sm90a (H100/H800)
    fa3: requires flash-attn 3
    layout(name): parametrized layout (bsnh or bnsh)
